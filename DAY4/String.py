# 숫자를 문자열로 형변환하여 + 연산자와 연결
# 문자열에서 사용되는 연산자 : + *

# * : 문자열을 곱하는 수만큼 반복생성
string = '파이썬'
result = string * 3
print(result)

# 인덱스 : 문자의 위치
# string[0] : 인덱스 0번째 문자 ( 첫 번째 문자 )
# string[-1] : 마지막 문자

# 슬라이싱 : 문자열 중에서 일부분을 추출하는 것
# 인덱스 사용
# string[n] : 인덱스 n번째 문자
# string[0:n] : 0부터 n-1번째까지의 문자열
# string[n:] : n부터 끝까지
# string[:n] : 0부터 n-1까지
