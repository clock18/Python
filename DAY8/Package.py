# 패키지(package) : 모듈들을 모아놓은 디렉토리(폴더)

# 일반적인 디렉토리와의 차이점
# 패키지는 디렉토리 안에 __init__.py 존재 ( 빈파일)

# 패키지를 사용할 경우 모듈  import 가능
# import 패키지.모듈명
# import 패키지.모듈명 as 별칭
# from 패키지.모듈 import 함수

# 패키지를 파이참에서 구성하는 방법
# [파일] or 작업하고 싶은 디렉토리 -> [New] -> [python package] -> package 이름 입력
# 패키지가 생성되면 __init__.py 확인

# import Mypack.Pack1       # 패키지 이름에 띄워쓰기가 있으면 참조 불가로 뜸