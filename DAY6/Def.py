# 함수 구조
# 변수는 괄호가 없고 함수는 괄호가 있다.

# 함수를 만들 때 (정의할 때) 사용하는 키워드 : def

# def 함수명(매개변수1, 매개변수2):
#     수행 문장1
#     수행 문장2
#     반환 값 ( 생략 가능 )

# 파이참에서는 함수 이름은 소문자 사용 권장
# 함수이름은 카멜형 or 스네이크형

# 함수 정의             # 반드시 정의하고 호출해야함. ( Definition )
# def show_hello():     # 함수는 호출하지 않으면 아무일도 하지 않는다.
#     print('hello')

# 함수 호출
# show_hello()