# 내장함수 ( built-in functions )
# 파이썬에 이미 만들어져 내장되어 있는 함수들
# 별도의 모듈을 import하지 않고 사용 가능

# 함수의 형식과 목적에 따라 호출해서 바로 사용 가능
# print(), input(), id(), type() 등
# 특정 객체를 통해 사용가능한 함수 ( 메소드 )
# 리스트.index(), append(), insert(), count(), remove() 등
# 문자열.index(), find(), sort(), reverse() 등

